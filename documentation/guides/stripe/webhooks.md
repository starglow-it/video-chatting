## Generate Stripe Webhook keys:

```shell
$ cd ./scripts
# Generate the local Stripe Webhook key
$ bash stripe-listen-local.sh webhook

# Generate the local Stripe Express Webhook key
$ bash stripe-listen-local.sh express-webhook
```
