import type { NextPage } from 'next';

import { NotFoundContainer } from '@containers/NotFoundContainer/NotFoundContainer';

const NotFoundPage: NextPage = (): JSX.Element => <NotFoundContainer />;

export default NotFoundPage;
